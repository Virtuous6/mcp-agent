$schema: ../schema/mcp-agent.config.schema.json

execution_engine: asyncio
logger:
  transports: [console, file]
  level: info
  show_progress: true
  path: "logs/meta-agent.jsonl"
  path_settings:
    path_pattern: "logs/meta-agent-{unique_id}.jsonl"
    unique_id: "timestamp"
    timestamp_format: "%Y%m%d_%H%M%S"

mcp:
  servers:
    # Core Slack integration
    slack:
      command: "npx"
      args: ["-y", "@modelcontextprotocol/server-slack"]

    # Supabase for data persistence, memory, and workflows
    supabase:
      command: "npx"
      args: ["-y", "@supabase/mcp-server-supabase@latest"]
      description: "Access Supabase API operations"

    # Essential utility servers
    filesystem:
      command: "npx"
      args: ["-y", "@modelcontextprotocol/server-filesystem"]

    fetch:
      command: "uvx"
      args: ["mcp-server-fetch"]

    # GitHub for code deployment and management
    github:
      command: "docker"
      args:
        [
          "run",
          "-i",
          "--rm",
          "-e",
          "GITHUB_PERSONAL_ACCESS_TOKEN",
          "ghcr.io/github/github-mcp-server",
        ]

    # Browser automation for complex workflows
    brave_search:
      command: "npx"
      args: ["-y", "@modelcontextprotocol/server-brave-search"]

openai:
  default_model: gpt-4o

human_input:
  enabled: true
